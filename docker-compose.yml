services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - SESSION_SECRET=${SESSION_SECRET:-change-this-to-a-long-random-string}
    volumes:
      - russian-data:/app/data

volumes:
  russian-data:
