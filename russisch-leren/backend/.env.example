PORT=3000
SESSION_SECRET=change-this-to-a-long-random-string

# Optional. Enables the "Vraag AI om uitleg" button on wrong answers.
# Without this set, the app works exactly as before -- the button is just hidden.
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
